<%= form_for(@accessory_point) do |f| %>
  <% if @accessory_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accessory_point.errors.count, "error") %> prohibited this accessory_point from being saved:</h2>

      <ul>
      <% @accessory_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
